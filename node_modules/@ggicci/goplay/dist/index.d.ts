export declare type CompilerEvent = {
    Delay: number;
    Kind: string;
    Message: string;
};
export declare type CompilerResponse = {
    Errors: string;
    Events: CompilerEvent[] | null;
};
export declare type CompilerVersion = 'gopre' | 'gotip';
export declare class GoPlayProxy {
    proxyUrl: string;
    constructor(proxyUrl?: string);
    raiseForStatus(resp: Response): Promise<void>;
    compile(sourceCode: string, goVersion?: CompilerVersion): Promise<CompilerResponse>;
    renderCompile(container: HTMLElement, sourceCode: string, goVersion?: CompilerVersion): Promise<void>;
    renderEvent(e: CompilerEvent): Promise<HTMLSpanElement>;
    renderMessage(kind: string, message: string): HTMLSpanElement;
    share(sourceCode: string, goVersion?: CompilerVersion): Promise<string>;
}
